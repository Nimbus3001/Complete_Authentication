/**
 * DEPRECATED
 * Port 3000 logic removed to resolve Issue #3 (Duplicate Server).
 * Primary backend is handled via app.js on Port 5000.
 */
console.warn('CRITICAL: server.js is inactive. Use "npm run start:backend" (app.js).');
